# Changelog

The `lattice@percena` plugin changelog is maintained at the repository root:

- [`../../CHANGELOG.md`](../../CHANGELOG.md) — Keep a Changelog + SemVer. The canonical plugin version lives in `.claude-plugin/plugin.json` and `../../.claude-plugin/marketplace.json`.
