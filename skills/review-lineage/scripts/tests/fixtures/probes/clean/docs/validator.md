# Validator

The validator (`tools/validate-lattice-artifacts.py`) emits `demo_code_one` when the demo drifts.
A config key like `queue_health_hours` on a line without the v-word is not a code citation.
