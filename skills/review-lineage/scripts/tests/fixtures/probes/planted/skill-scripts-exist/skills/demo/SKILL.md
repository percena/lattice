---
name: demo
description: fixture skill — names a script that does not exist.
---

# demo

Run `bash "$SKILL_ROOT/scripts/demo.sh"`, then `bash "$SKILL_ROOT/scripts/ghost.sh"` (planted: no such file).
